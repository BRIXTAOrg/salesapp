import 'package:flutter/material.dart';import '../../../core/models/mobile_capability.dart';
class DynamicCapabilityScreen extends StatelessWidget{const DynamicCapabilityScreen({super.key,required this.capability});final MobileCapability capability;@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:Text(capability.title)),body:Padding(padding:const EdgeInsets.all(18),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(capability.title,style:Theme.of(context).textTheme.headlineSmall),const SizedBox(height:8),Text(capability.description??'Assigned by management.'),const SizedBox(height:20),Card(child:ListTile(leading:const Icon(Icons.extension_outlined),title:Text(capability.type.replaceAll('_',' ')),subtitle:const Text('Flow 1: assignment/rendering is live. Business action engine plugs in next.')))])));}
